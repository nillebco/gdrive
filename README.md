# Upload a markdown file and get it converted to Google Docs by the Google API

fnox exec uv run main.py ../domains.md --destination-mimetype "application/vnd.google-apps.document" --source-mimetype text/markdown
